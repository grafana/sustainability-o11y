name: Build and Push on Merge

on:
  push:
    branches:
      - main
    paths:
      - "azure-carbon-exporter/**"

permissions:
  contents: read

jobs:
  build-and-export-digest:
    permissions:
      contents: read
      id-token: write
    outputs:
      images: ${{ steps.build.outputs.images }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runnerType: ubuntu-24.04-arm
            platform: linux/arm64
          - runnerType: ubuntu-24.04
            platform: linux/amd64
    runs-on: ${{ matrix.runnerType }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Build and Push Image
        id: build
        uses: grafana/shared-workflows/actions/docker-build-push-image@438c8d6321147607b3f6f0e922508e78e746af9b # docker-build-push-image/v0.3.1
        with:
          registries: dockerhub
          platforms: ${{ matrix.platform }}
          context: azure-carbon-exporter
          file: azure-carbon-exporter/Dockerfile
          outputs: "type=image,push-by-digest=true,name-canonical=true,push=true"
          push: true
          tags: |
            type=raw,value=latest
            type=sha,prefix=main-,format=short
          include-tags-in-push: false

      - name: Export Docker Digest
        uses: grafana/shared-workflows/actions/docker-export-digest@438c8d6321147607b3f6f0e922508e78e746af9b # docker-export-digest/v0.3.1
        with:
          digest: ${{ steps.build.outputs.digest }}
          platform: ${{ matrix.platform }}

  push-manifest:
    needs: build-and-export-digest
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Push Multi-Arch Manifest
        uses: grafana/shared-workflows/actions/docker-import-digests-push-manifest@438c8d6321147607b3f6f0e922508e78e746af9b # docker-import-digests-push-manifest/v0.3.1
        with:
          images: ${{ needs.build-and-export-digest.outputs.images }}
          push: true
          tags: |
            type=raw,value=latest
            type=sha,prefix=main-,format=short

  deploy:
    needs: push-manifest
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    steps:
      - name: Trigger Argo Workflow
        uses: grafana/shared-workflows/actions/trigger-argo-workflow@438c8d6321147607b3f6f0e922508e78e746af9b # trigger-argo-workflow/v0.3.1
        with:
          namespace: platform-monitoring-cd
          workflow_template: azure-carbon-exporter
          parameters: |
            imageName=grafana/azure-carbon-exporter
            dockertag=main-${{ github.sha }}
            prCommentContext= Triggered by ${{ github.sha }} in grafana/sustainability-o11y
            commit_author=grafana-delivery-bot
